import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color.fromARGB(216, 9, 6, 177);
  static const Color primaryDark = Color.fromARGB(255, 32, 2, 102);
  static const Color appbarcolor = Color.fromARGB(218, 13, 10, 61);
  static const Color appbarTextcolor = Color(0xFFFFFFFF);
  static const Color appbariconcolor = Color(0xFFFFFFFF);
  static const Color iconcolor = Color.fromARGB(255, 0, 0, 0);

  // Accent / Secondary
  static const Color secondary = Color(0xFF03DAC6);

  // Backgrounds
  static const Color background = Color(0xFFF5F5F5);
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Text colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF757575);

  static Color buttonColor() {
    return const Color(0xFF6200EE);
  }

  // Error
  static const Color error = Color(0xFFB00020);
}
